// import * as fromCurrentAction from './current-action.actions';

describe('loadCurrentActions', () => {
  it('should return an action', () => {
    // expect(fromCurrentAction.loadCurrentActions().type).toBe('[CurrentAction] Load CurrentActions');
  });
});
