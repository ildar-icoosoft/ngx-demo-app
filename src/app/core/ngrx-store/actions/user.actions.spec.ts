// import * as fromUserAction from './user.actions';

describe('loadUserActions', () => {
  it('should return an action', () => {
    // expect(fromUserAction.loadUserActions().type).toBe('[UserAction] Load UserActions');
  });
});
